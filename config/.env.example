# Supabase Configuration
# Copy this file to .env and fill in your actual values

# Supabase Project URL
# Get this from your Supabase project settings
VITE_SUPABASE_URL=https://your-project.supabase.co

# Supabase Anonymous Key
# This is safe to use in client-side code
# Get this from your Supabase project settings > API
VITE_SUPABASE_ANON_KEY=your_anon_key_here

# Supabase Service Role Key (OPTIONAL - only for server-side operations)
# WARNING: Keep this secret! Never commit to version control or expose in client-side code
# Only use this for backend/admin operations
VITE_SUPABASE_SERVICE_ROLE_KEY=your_service_role_key_here

# ============================================================
# Database Selection (Development Only)
# ============================================================
# Force which database to use during development
# - Set to 'true' to force cloud database (even when running on localhost)
# - Set to 'false' to force local database (requires Supabase running locally)
# - Leave unset or comment out for auto-detection based on hostname
#
# Usage:
#   VITE_USE_CLOUD_DB=true npm run dev    # Use cloud DB
#   VITE_USE_CLOUD_DB=false npm run dev   # Use local DB
#   npm run dev                            # Auto-detect
#
# Or use the convenience scripts:
#   npm run dev:cloud   # Force cloud database
#   npm run dev:local   # Force local database
#   npm run dev         # Auto-detect (default)
#
# Or use start.sh with flags:
#   ./start.sh --cloud  # Force cloud database
#   ./start.sh --local  # Force local database
#   ./start.sh          # Auto-detect (default)
#
# NOTE: In production builds, this is always ignored and cloud database is used
# VITE_USE_CLOUD_DB=true
